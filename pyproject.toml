[project]
name = "pbl-aurora"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JFrois", email = "juan.frois9@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "matplotlib>=3.7.0",
    "numpy>=1.24.0",
    "pandas>=2.0.0",
    "pytest>=7.4.0",
    "requests>=2.34.2",
    "scikit-learn>=1.3.0",
]

[project.scripts]
pbl-aurora = "pbl_aurora:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
